import 'package:flutter/material.dart';
import 'package:nutes/core/services/repository.dart';

class SelfProfileScreen extends StatelessWidget {
  final user = Repo.currentProfile;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
